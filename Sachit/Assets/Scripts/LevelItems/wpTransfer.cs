using UnityEngine;
using System.Collections;

public class wpTransfer : MonoBehaviour {
	/*
	public int grpItems = 0;
	public bool doUpdate = false;

	
	public Transform  wpSource;
	public Transform  wpTarget;
	
	private wpAttribs[] sourceWPScr;
	private wpAttribs[] targetWPScr;
	

	void OnDrawGizmosSelected () 
	{
		if (doUpdate)
		{
				// get the randerers of all children
			sourceWPScr = wpSource.GetComponentsInChildren<wpAttribs>();
			targetWPScr = wpTarget.GetComponentsInChildren<wpAttribs>();
			grpItems = sourceWPScr.Length;

			if (sourceWPScr != null)
			{
					// fill the array with our renderers
				for (var i = 0; i < grpItems; i++)
				{
					targetWPScr[i].adjustColor = sourceWPScr[i].adjustColor;
				}
				
				Debug.Log (grpItems + " Color Transfered!!! ");
			}
			else
			{
				Debug.Log ("There are no Renderes in this group! Nothing added");
			}

				// make sure this is run ONCE
			doUpdate = false;
		}
	}
	 */
}
